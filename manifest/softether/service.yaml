apiVersion: v1
kind: Service
metadata:
  name: se-service
  annotations:
    service.beta.kubernetes.io/vultr-loadbalancer-protocol: "http"
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: se-service
  ports:
    - name: http
      port: 80
      targetPort: 443
